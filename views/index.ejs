<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TransactEase - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .hero-section {
            background: linear-gradient(to right, #212529, #343a40);
            color: white;
            padding: 100px 0;
            text-align: center;
        }
        .hero-section h1 {
            font-size: 3rem;
            font-weight: bold;
        }
        .hero-section h3 {
            color: #f8d210;
            font-weight: 500;
        }
        .btn-custom {
            background-color: #f8d210; 
            color: white !important; 
            border: 2px solid black;
            font-weight: bold;
            padding: 12px 24px;
            font-size: 1.2rem;
            transition: all 0.3s ease-in-out;
        }
        .btn-custom:hover {
            background-color: white; 
            color: #f8d210 !important; 
            border: 2px solid #f8d210;
        }
    </style>
</head>
<body>
    <%- include('header.ejs') %>

    <div class="hero-section">
        <div class="container">
            <h1>Build Your Financial Edge with Precision</h1>
            <h3>Seamless Currency Conversion at Your Fingertips</h3>
            <div class="mt-4">
                <form action="Conversion" method="get" class="d-inline">
                    <button type="submit" style="font-weight: 600;" class="btn btn-custom btn-lg">
                         Convert Currency
                    </button>
                    </form>
                <form action="Exchangerates" method="get" class="d-inline">
                    <button type="submit" style="font-weight: 600;" class="btn btn-custom btn-lg">
                         Live Exchange Rates
                    </button>
                </form>
            </div>
        </div>
    </div>
    
    <%- include('footer.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>   
</body>
</html>

