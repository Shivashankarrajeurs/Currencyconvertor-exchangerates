<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Currency Conversion Result</title>

  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>

  
  <style>
    body {
      background: linear-gradient(to right, #e0eafc, #cfdef3);
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
    }
    .result-container {
      max-width: 600px;
      margin: 60px auto;
      background: white;
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }
    .result-container h1 {
      font-size: 1.8rem;
      margin-bottom: 20px;
      color: #343a40;
    }
    .result-container p {
      font-size: 1.1rem;
      margin: 8px 0;
    }
    .btn-back {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <%- include('header.ejs') %>

  <div class="container">
    <div class="result-container">
      <h1 class="text-center">Currency Conversion Result</h1>

      <% if (success) { %>
        <p><strong>From:</strong> <%= fromCurrency || "N/A" %></p>
        <p><strong>To:</strong> <%= toCurrency || "N/A" %></p>
        <p><strong>Amount:</strong> <%= amount || "N/A" %></p>
        <p><strong>Exchange Rate:</strong> <%= rate || "Unavailable" %></p>
        <p><strong>Converted Amount:</strong> <%= result || "Error" %> <%= toCurrency || "N/A" %></p>
        <p><strong>Date:</strong> <%= date || "Invalid Date" %></p>
      <% } else { %>
        <div class="alert alert-danger text-center" role="alert">
          Conversion failed. Please try again.
        </div>
      <% } %>

      <div class="text-center btn-back">
        <a href="/Conversion" class="btn btn-outline-primary">Go Back</a>
      </div>
    </div>
  </div>

  <%- include('footer.ejs') %>
</body>
</html>
